server:
  port: 8082


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employee-service
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
